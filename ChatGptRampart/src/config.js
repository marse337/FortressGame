export const CONFIG = {
  // Bigger internal “console” buffer (still 4:3, still divisible by TILE=8)
  INTERNAL_W: 384,
  INTERNAL_H: 288,
  TILE: 8,

  // Bigger on-page canvas
  VIEW_MAX_W: 1200,
  VIEW_MIN_W: 720,
  VIEW_PADDING: 32,

  ASPECT: 4 / 3,
};
